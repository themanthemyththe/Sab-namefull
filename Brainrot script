--===== STEAL A BRAINROT AUTO-GRAB + MOBILE MENU =====--

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local rootPart = character:WaitForChild("HumanoidRootPart")

-- ===== CONFIGURATION =====
local CONFIG = {
    -- Auto-Grab Settings
    ENABLED = true,
    GRAB_RANGE = 50,
    GRAB_SPEED = 0.5,
    
    -- Desync Settings
    DESYNC_ENABLED = true,
    DESYNC_AMOUNT = 15,
    
    -- Booster Settings
    SPEED_BOOST = 2.5,  -- Increased for noticeable effect
    JUMP_POWER = 75,    -- Increased for noticeable effect
    
    -- Visual Settings
    SHOW_RING = true,
    RING_COLOR = Color3.fromRGB(0, 255, 100),
    RING_TRANSPARENCY = 0.5,
    
    -- Keybinds
    TOGGLE_KEY = Enum.KeyCode.E,
    SPEED_BOOST_KEY = Enum.KeyCode.LeftShift,
    JUMP_BOOST_KEY = Enum.KeyCode.Space,
    DESYNC_TOGGLE_KEY = Enum.KeyCode.R,  -- Added desync toggle key
}

-- ===== VARIABLES =====
local autoGrabEnabled = CONFIG.ENABLED
local desyncEnabled = CONFIG.DESYNC_ENABLED
local speedBoostActive = false
local visualRing = nil
local lastGrabTime = 0
local grabCooldown = 0.1
local originalWalkSpeed = 16
local originalJumpPower = 50

-- ===== FUNCTIONS =====

-- Create visual ring around player
local function createVisualRing()
    if visualRing then visualRing:Destroy() end
    
    -- Create a thin ring instead of a solid cylinder
    local ring = Instance.new("Part")
    ring.Shape = Enum.PartType.Cylinder
    ring.Material = Enum.Material.Neon
    ring.CanCollide = false
    ring.Anchored = false
    ring.CFrame = rootPart.CFrame * CFrame.Angles(math.pi/2, 0, 0)
    ring.Size = Vector3.new(CONFIG.GRAB_RANGE * 2, 0.1, CONFIG.GRAB_RANGE * 2)  -- Thin ring
    ring.Color = CONFIG.RING_COLOR
    ring.Transparency = CONFIG.RING_TRANSPARENCY
    ring.BrickColor = BrickColor.new("Lime green")
    
    -- Add glow effect
    local attachment = Instance.new("Attachment")
    attachment.Parent = ring
    
    local trail = Instance.new("Trail")
    trail.Attachment0 = attachment
    trail.Attachment1 = attachment
    trail.Color = ColorSequence.new(CONFIG.RING_COLOR)
    trail.Transparency = NumberSequence.new(0.3)
    trail.Lifetime = 0.1
    trail.Parent = ring
    
    -- Weld to player
    local weld = Instance.new("WeldConstraint")
    weld.Part0 = rootPart
    weld.Part1 = ring
    weld.Parent = ring
    
    ring.Parent = Workspace
    visualRing = ring
end

-- Update ring position
local function updateRing()
    if visualRing and visualRing.Parent then
        visualRing.CFrame = rootPart.CFrame * CFrame.Angles(math.pi/2, 0, 0)
    end
end

-- Find nearest brainrot
local function findNearestBrainrot()
    local nearest = nil
    local nearestDistance = CONFIG.GRAB_RANGE
    
    -- Search in workspace and all children
    local function searchObjects(parent)
        for _, obj in pairs(parent:GetChildren()) do
            if obj:IsA("Model") and obj.Name:lower():find("brainrot") then
                local targetPart = obj:FindFirstChild("HumanoidRootPart") or obj.PrimaryPart or obj:FindFirstChild("Head") or obj:FindFirstChild("Torso")
                if targetPart then
                    local distance = (targetPart.Position - rootPart.Position).Magnitude
                    if distance < nearestDistance then
                        nearestDistance = distance
                        nearest = obj
                    end
                end
            elseif obj:IsA("Part") and obj.Name:lower():find("brainrot") then
                local distance = (obj.Position - rootPart.Position).Magnitude
                if distance < nearestDistance then
                    nearestDistance = distance
                    nearest = obj
                end
            end
            
            -- Search deeper
            if #obj:GetChildren() > 0 then
                searchObjects(obj)
            end
        end
    end
    
    searchObjects(Workspace)
    return nearest
end

-- Auto-grab brainrot
local function autoGrabBrainrot()
    if not autoGrabEnabled or tick() - lastGrabTime < grabCooldown then return end
    
    local target = findNearestBrainrot()
    if not target then return end
    
    lastGrabTime = tick()
    
    -- Find target position
    local targetPart = target:FindFirstChild("HumanoidRootPart") or target.PrimaryPart or target:FindFirstChild("Head") or target:FindFirstChild("Torso")
    if not targetPart then return end
    
    local targetPosition = targetPart.Position
    
    -- Apply desync if enabled
    if desyncEnabled then
        targetPosition = targetPosition + Vector3.new(
            math.random(-CONFIG.DESYNC_AMOUNT, CONFIG.DESYNC_AMOUNT),
            math.random(-CONFIG.DESYNC_AMOUNT, CONFIG.DESYNC_AMOUNT),
            math.random(-CONFIG.DESYNC_AMOUNT, CONFIG.DESYNC_AMOUNT)
        )
    end
    
    -- Smooth movement to target
    rootPart.CFrame = rootPart.CFrame:Lerp(CFrame.new(targetPosition), CONFIG.GRAB_SPEED)
end

-- Speed boost
local function activateSpeedBoost()
    if speedBoostActive then return end
    speedBoostActive = true
    humanoid.WalkSpeed = originalWalkSpeed * CONFIG.SPEED_BOOST
    speedBtn.Text = "Speed Boost (ON)"
    print("✓ Speed Boost ACTIVATED - Speed: " .. humanoid.WalkSpeed)
end

local function deactivateSpeedBoost()
    if not speedBoostActive then return end
    speedBoostActive = false
    humanoid.WalkSpeed = originalWalkSpeed
    speedBtn.Text = "Speed Boost"
    print("✗ Speed Boost DEACTIVATED")
end

-- Jump boost
local function jumpBoost()
    -- Save original jump power
    local oldJumpPower = humanoid.JumpPower
    humanoid.JumpPower = CONFIG.JUMP_POWER
    humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    
    -- Apply additional force for higher jumps
    task.wait(0.05)
    if rootPart and rootPart.AssemblyLinearVelocity then
        rootPart.AssemblyLinearVelocity = Vector3.new(
            rootPart.AssemblyLinearVelocity.X,
            CONFIG.JUMP_POWER * 1.5,
            rootPart.AssemblyLinearVelocity.Z
        )
    end
    
    -- Restore jump power after jump
    task.wait(0.1)
    humanoid.JumpPower = oldJumpPower
    print("↑ Jump Boost! - Height: " .. CONFIG.JUMP_POWER)
end

-- Toggle auto-grab
local function toggleAutoGrab()
    autoGrabEnabled = not autoGrabEnabled
    autoBtn.Text = "Auto-Grab: " .. (autoGrabEnabled and "ON" or "OFF")
    print("Auto-Grab: " .. (autoGrabEnabled and "ON ✓" or "OFF ✗"))
end

-- Toggle desync
local function toggleDesync()
    desyncEnabled = not desyncEnabled
    desyncBtn.Text = "Desync: " .. (desyncEnabled and "ON" or "OFF")
    print("Desync Mode: " .. (desyncEnabled and "ON ✓" or "OFF ✗"))
end

-- Toggle ring
local function toggleRing()
    CONFIG.SHOW_RING = not CONFIG.SHOW_RING
    ringBtn.Text = "Ring: " .. (CONFIG.SHOW_RING and "ON" or "OFF")
    
    if CONFIG.SHOW_RING then
        createVisualRing()
        print("✓ Ring Enabled")
    else
        if visualRing then
            visualRing:Destroy()
            visualRing = nil
        end
        print("✗ Ring Disabled")
    end
end

-- ===== INPUT HANDLING =====
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    
    if input.KeyCode == CONFIG.TOGGLE_KEY then
        toggleAutoGrab()
    elseif input.KeyCode == CONFIG.DESYNC_TOGGLE_KEY then
        toggleDesync()
    elseif input.KeyCode == CONFIG.SPEED_BOOST_KEY and not speedBoostActive then
        activateSpeedBoost()
    elseif input.KeyCode == CONFIG.JUMP_BOOST_KEY then
        jumpBoost()
    end
end)

UserInputService.InputEnded:Connect(function(input, gameProcessed)
    if input.KeyCode == CONFIG.SPEED_BOOST_KEY then
        deactivateSpeedBoost()
    end
end)

-- ===== MAIN LOOP =====
RunService.RenderStepped:Connect(function()
    if not character or not character.Parent then return end
    
    if CONFIG.SHOW_RING and visualRing then
        updateRing()
    end
    
    if autoGrabEnabled then
        autoGrabBrainrot()
    end
end)

-- Handle character respawn
player.CharacterAdded:Connect(function(newCharacter)
    character = newCharacter
    humanoid = character:WaitForChild("Humanoid")
    rootPart = character:WaitForChild("HumanoidRootPart")
    
    -- Save original values
    originalWalkSpeed = humanoid.WalkSpeed
    originalJumpPower = humanoid.JumpPower
    
    print("Script reinitialized on respawn")
    
    if CONFIG.SHOW_RING then
        task.wait(1)  -- Wait for character to fully load
        createVisualRing()
    end
end)

--===== MOBILE MENU =====--
task.wait(1) -- Small delay to ensure character is loaded

local gui = Instance.new("ScreenGui")
gui.ResetOnSpawn = false
gui.IgnoreGuiInset = true
gui.Parent = player:WaitForChild("PlayerGui")

-- Main frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 250, 0, 310)  -- Made taller for new button
frame.Position = UDim2.new(0.05, 0, 0.2, 0)
frame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
frame.BorderSizePixel = 2
frame.BorderColor3 = Color3.fromRGB(0, 255, 100)
frame.Active = true
frame.Draggable = true
frame.Parent = gui

-- Title bar
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 36)
title.BackgroundColor3 = Color3.fromRGB(0, 50, 25)
title.BorderSizePixel = 0
title.Text = "🧠 BRAINROT CONTROLLER"
title.TextColor3 = Color3.fromRGB(0, 255, 100)
title.Font = Enum.Font.GothamBold
title.TextSize = 18
title.Parent = frame

-- Button creator
local function makeButton(text, y, color)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -20, 0, 38)
    btn.Position = UDim2.new(0, 10, 0, y)
    btn.BackgroundColor3 = color or Color3.fromRGB(35, 35, 35)
    btn.BorderSizePixel = 1
    btn.BorderColor3 = Color3.fromRGB(0, 255, 100)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 15
    btn.Text = text
    btn.AutoButtonColor = false
    btn.Parent = frame

    btn.MouseEnter:Connect(function()
        btn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    end)

    btn.MouseLeave:Connect(function()
        btn.BackgroundColor3 = color or Color3.fromRGB(35, 35, 35)
    end)

    return btn
end

-- Buttons
local autoBtn = makeButton("🤖 Auto-Grab: ON", 45, Color3.fromRGB(40, 40, 40))
local speedBtn = makeButton("⚡ Speed Boost", 90, Color3.fromRGB(40, 40, 40))
local jumpBtn = makeButton("🦘 Jump Boost", 135, Color3.fromRGB(40, 40, 40))
local ringBtn = makeButton("🔮 Ring: ON", 180, Color3.fromRGB(40, 40, 40))
local desyncBtn = makeButton("🌀 Desync: ON", 225, Color3.fromRGB(40, 40, 40))

-- Button connections
autoBtn.MouseButton1Click:Connect(function()
    toggleAutoGrab()
end)

speedBtn.MouseButton1Click:Connect(function()
    if speedBoostActive then
        deactivateSpeedBoost()
    else
        activateSpeedBoost()
    end
end)

jumpBtn.MouseButton1Click:Connect(function()
    jumpBoost()
end)

ringBtn.MouseButton1Click:Connect(function()
    toggleRing()
end)

desyncBtn.MouseButton1Click:Connect(function()
    toggleDesync()
end)

-- ===== INITIALIZATION =====
-- Save original values
originalWalkSpeed = humanoid.WalkSpeed
originalJumpPower = humanoid.JumpPower

if CONFIG.SHOW_RING then
    task.wait(0.5)
    createVisualRing()
end

-- Update button texts
autoBtn.Text = "🤖 Auto-Grab: " .. (autoGrabEnabled and "ON" or "OFF")
desyncBtn.Text = "🌀 Desync: " .. (desyncEnabled and "ON" or "OFF")
ringBtn.Text = "🔮 Ring: " .. (CONFIG.SHOW_RING and "ON" or "OFF")

print("════════════════════════════════════")
print("🧠 Steal a Brainrot Auto-Grab LOADED ✓")
print("════════════════════════════════════")
print("CONTROLS:")
print("  E - Toggle Auto-Grab")
print("  R - Toggle Desync Mode")
print("  Hold Shift - Speed Boost (" .. CONFIG.SPEED_BOOST .. "x)")
print("  Space - Jump Boost (" .. CONFIG.JUMP_POWER .. ")")
print("  Mobile Menu - Touch controls available")
print("════════════════════════════════════")
print("CURRENT SETTINGS:")
print("  Range: " .. CONFIG.GRAB_RANGE .. " studs")
print("  Speed Boost: " .. CONFIG.SPEED_BOOST .. "x")
print("  Jump Power: " .. CONFIG.JUMP_POWER)
print("  Desync: " .. (desyncEnabled and "ON ✓" or "OFF ✗"))
print("════════════════════════════════════")